 # Generated by https://jlopp.github.io/bitcoin-core-config-generator/

# This config should be placed in following path:
# %UserProfile%\AppData\Roaming\Bitcoin\bitcoin.conf

# [core]
# Tell peer nodes not to send transactions to you that pay less than your mempool min fee.
feefilter=0
# Keep the transaction memory pool below <n> megabytess.
maxmempool=1000
# Do not keep transactions in the mempool longer than <n> hours.
mempoolexpiry=336
# Reindex chain state from the currently indexed blocks. WARNING: very slow!
#reindex-chainstate=1
# Rebuild chain state and block index from the blk*.dat files on disk. WARNING: very slow!
#reindex=1
# Maintain a full transaction index, used by the getrawtransaction rpc call.
txindex=1





# [debug]
# Do not accept transactions if number of in-mempool ancestors is <n> or more.
limitancestorcount=500
# Do not accept transactions whose size with all in-mempool ancestors exceeds <n> kilobytes.
limitancestorsize=500
# Do not accept transactions if any ancestor would have <n> or more in-mempool descendants.
limitdescendantcount=500
# Do not accept transactions whose size with all in-mempool descendants exceeds <n> kilobytes.
limitdescendantsize=500
# Log IP Addresses in debug output.
logips=1
# Log timestamps in debug output.
logtimestamps=0
# Log timestamps with microsecond precision.
logtimemicros=1
# Log transaction fee per kB when mining blocks.
printpriority=1

# [rpc]
# Username for JSON-RPC connections
rpcuser=secret_username
# Password for JSON-RPC connections
rpcpassword=secret_password
# Listen for JSON-RPC connections on this port
rpcport=1234
server=1 # Accept RPCs 
listen=1 # Accept incoming connections despite explicit connects. 
rpcallowip=127.0.0.1 # Allow ANYONE to RPC us. 
rpcbind=127.0.0.1:50011 # Allow ANYONE to RPC us. 




datadir=/media/hdd/itay/bitcoinData
 
